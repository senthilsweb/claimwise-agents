[project]
name = "claimwise-chat-adapter"
version = "0.1.0"
description = "FastAPI bridge: mcp-chat-client widget contract -> Bedrock AgentCore InvokeAgentRuntime"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn>=0.30",
    "boto3>=1.34",
    "python-dotenv>=1.0",
]

[tool.uv]
package = false
